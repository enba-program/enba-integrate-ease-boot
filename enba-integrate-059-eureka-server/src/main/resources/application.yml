server:
  port: 8059

spring:
  application:
    name: @project.artifactId@

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    enable-self-preservation: false